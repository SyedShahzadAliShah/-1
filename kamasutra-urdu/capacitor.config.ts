import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.couples.kamasutra.urdu',
  appName: 'قربت کی تعلیم',
  webDir: 'dist'
};

export default config;
